# API base URL used by the portfolio contact form
# Netlify: set this as a Site environment variable for each context (production, deploy preview, branch deploy)
VITE_API_URL=https://saumya-backend.onrender.com
